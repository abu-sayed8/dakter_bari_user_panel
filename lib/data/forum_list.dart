import 'package:users_panel/model/forum_model.dart';

List<ForumData> getForum(){
  List<ForumData> list = List();
  ForumData forumData = ForumData();

  forumData.questions = 'Do Bengali people watch Bangla-Anitubers on YouTube?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'DRegarding COVID-19. If the government (UK) had simply '
      'done nothing at all and treated COVID-19 at it does a cold or ordinary '
      'flue. What would have happened? What condition would we be in now?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'What is the best way to register a company in Delaware without actually living in Delaware?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'Do Bengali people watch Bangla-Anitubers on YouTube?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'DRegarding COVID-19. If the government (UK) had simply '
      'done nothing at all and treated COVID-19 at it does a cold or ordinary '
      'flue. What would have happened? What condition would we be in now?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'What is the best way to register a company in Delaware without actually living in Delaware?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'Do Bengali people watch Bangla-Anitubers on YouTube?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'DRegarding COVID-19. If the government (UK) had simply '
      'done nothing at all and treated COVID-19 at it does a cold or ordinary '
      'flue. What would have happened? What condition would we be in now?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'What is the best way to register a company in Delaware without actually living in Delaware?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'Do Bengali people watch Bangla-Anitubers on YouTube?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  forumData = ForumData();
  forumData.questions = 'Do Bengali people watch Bangla-Anitubers on YouTube?';
  forumData.ansNum = '7 Answers · ';
  list.add(forumData);

  return list;
}